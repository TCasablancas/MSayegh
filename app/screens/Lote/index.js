import React from 'react';
import {
    ScrollView,
    View,
    Text,
} from 'react-native';

import styles from './styles';

class Lote extends React.Component {

    render() {
        return(
            <ScrollView>
                
            </ScrollView>
        )
    }
}

export default Lote;